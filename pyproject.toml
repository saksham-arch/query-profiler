[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "query-profiler"
version = "0.1.0"
description = "Privacy-conscious DB-API query measurements"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

